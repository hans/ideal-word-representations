# phone-sequence data representation
# sampling strategy: uniform over word types

data:
  sampler:
    strategy: word
    weights: uniform

  representation:
    form: phone-sequence
    drop_extras: true